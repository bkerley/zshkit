if [[ -x `which git` ]]; then
	mydir=`dirname $0`
	if [[ $mydir = . ]]; then
		mydir=`pwd`
	fi
	
	function zshkit-update () {
		pushd $mydir
		git pull
		source ~/.zshrc
		source ~/.zshenv
		popd
	}
fi